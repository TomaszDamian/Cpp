#include <iostream>

using namespace std;

int main()
{
    /*
    //find largest number out of 4 (shitty if statement version)
    int n1;
    int n2;
    int n3;
    int n4;

    cout << "Input your four number : ";
    cin >> n1 >> n2 >> n3 >> n4;

    if(n1 >= n2 && n1 >= n3 && n1 > n4)
    {
        cout << "Largest number: " << n1;
    }

    if(n2 >= n1 && n2 >= n3 && n2 > n4)
    {
        cout << "Largest number: " << n2;
    }

    if(n3 >= n1 && n3 >= n2 && n3 > n4) {
        cout << "Largest number: " << n3;
    }

    if(n4 >= n1 && n4 >= n2 && n4 > n3) {
        cout << "Largest number: " << n4;
    }

    //put in number and immidetly find out the sum
    int Output = 0;
    int InputNumber;
    for(int i = 0; i <= 4; i++){
        cout << "Input the number: ";
        cin >> InputNumber;
        Output = Output + InputNumber;
        cout << Output << endl;
        //cout << Output << endl;
    };

    //print from 10 down to 0
    for(int i = 10; i > 0; i--){
        cout << i << endl;
    };

    //find average for 5 numbers
    float nInput;
    float Total;
    float AmountOfnumbers;
    float Average;

    for(int counter = 0; counter <= 4; counter++){
        cout << "Input your number: ";
        cin >> nInput;
        Total = Total + nInput;
        AmountOfnumbers++;
    };
    Average = Total / AmountOfnumbers;
    cout << "you input " << AmountOfnumbers << " numbers" << endl;
    cout << "the average of all the numbers is: " << Average << endl;*/
}
